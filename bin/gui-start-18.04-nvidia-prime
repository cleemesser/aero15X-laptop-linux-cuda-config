#!/usr/bin/env bash

sudo rm /etc/X11/xorg.conf
sudo ln -s /etc/X11/xorg.conf.nvidia-prime /etc/X11/xorg.conf 

export CHRIS_USING_PRIME=1
# need to have nvidia loaded I assume
# nvidia-on-bbswitch # not sure if this works
startx
